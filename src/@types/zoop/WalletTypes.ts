export type WalletTypes = {
  data: {
    id: string
    holder: string
    balance: string
    currency: string
    routing_number: string
    number: string
    status: string
    metadata: string
    created_at: string
    updated_at: string
    primary: boolean
    uri: string
    resource: string
  }
}
